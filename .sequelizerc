const path = require('path');

module.exports = {
    'config': path.resolve('src', 'config', 'dbConfig.json'),
    'migrations-source-path': path.resolve('src', 'migrations'),
    'migrations-compiled-path': path.resolve('src', 'migrations', 'compiled')
};